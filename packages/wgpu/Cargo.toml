[package]
name = "hancer-gpu"
version = "0.1.0"
edition = "2021"

[dependencies]
wgpu = "24"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
pollster = "0.4"

[profile.release]
opt-level = 3
lto = true
