{
  "name": "hance",
  "version": "0.1.0",
  "type": "module",
  "description": "CLI that applies cinematic film effects to video and images in one FFmpeg pass",
  "bin": {
    "hance": "packages/cli/src/cli.ts"
  },
  "files": [
    "packages/core/src/**/*.ts",
    "packages/core/package.json",
    "packages/cli/src/**/*.ts",
    "packages/cli/package.json",
    "packages/ui/src/**/*.ts",
    "packages/ui/dist/**",
    "packages/ui/server.ts",
    "packages/ui/package.json",
    "packages/ui/public/**",
    "presets/**",
    "entitlements.plist",
    "LICENSE",
    "README.md"
  ],
  "keywords": ["video", "film", "ffmpeg", "color-grading", "cli"],
  "license": "FSL-1.1-MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Orva-Studio/hance"
  },
  "workspaces": ["packages/core", "packages/cli", "packages/ui"],
  "scripts": {
    "start": "bun run packages/cli/src/cli.ts",
    "build": "bun run build:wgpu && bun run build:ui && bun build packages/cli/src/cli.ts --compile ${BUN_TARGET:+--target=$BUN_TARGET} --define HANCE_VERSION=\"'${HANCE_VERSION:-dev}'\" --outfile hance",
    "build:ui": "bun run scripts/build-ui.ts",
    "build:wgpu": "cd packages/wgpu && ~/.cargo/bin/cargo build --release",
    "release:local": "HANCE_VERSION=${HANCE_VERSION:-0.0.0-local} bun run build && bun run scripts/stage-release.ts && bun run scripts/pack-local.ts",
    "release": "bun run scripts/release.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
