{
  "name": "openhancer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --compile --outfile openhancer",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
